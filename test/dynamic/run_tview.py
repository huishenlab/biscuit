import logging

logger = logging.getLogger(__name__)

def main(dir, bed_dir, old, new, ref, force):
    logger.warning('This is a placeholder function. `biscuit tview` does not test well at this time. Set `run.tview = false`')
